include(../utility.cmake)

set(list_of_subdirs "")
list_subdirectories(list_of_subdirs ${CMAKE_CURRENT_SOURCE_DIR} 1)
foreach(dir ${list_of_subdirs})
	if(dir MATCHES ".svn")
	else()
		add_subdirectory(${dir})
		message("added subdirectory: ${dir}")
	endif()
endforeach()
